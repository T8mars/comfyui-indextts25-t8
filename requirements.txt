# Extra runtime dependencies on top of a complete ComfyUI installation.
# ComfyUI already provides torch/torchaudio, einops, safetensors, sentencepiece,
# scipy, tqdm, filelock and the Hugging Face stack.
accelerate>=1.8.1
fugashi>=1.2.0
librosa>=0.10.2
munch>=4.0.0
omegaconf>=2.3.0
tiktoken>=0.7.0
# Tested with Transformers 4.52.1 and 4.57.6.
transformers>=4.52.1,<5
unidic-lite>=1.0
# Optional Chinese number/date normalization is listed separately in
# requirements-text-normalization.txt so a missing platform wheel never blocks
# the core ComfyUI node installation.
